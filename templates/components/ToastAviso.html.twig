{% for label, messages in app.flashes(['Aviso']) %}
	{% for message in messages %}
		<div class="toast-container" style="position: absolute; top: 10px; right: 10px;">
			<div class="toast fade show bg-warning">
				<div class="toast-header text-black bg-warning">
					<strong class="me-auto">
						{{ label }}</strong>
					<button type="button" class="btn-close btn-close-black" data-bs-dismiss="toast"></button>
				</div>
				<div class="toast-body text-black bg-warning">
					{{ message }}
				</div>
			</div>
		</div>
	{% endfor %}
{% endfor %}
